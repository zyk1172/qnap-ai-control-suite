import { request } from "../client.js";
import { register, z } from "./register.js";
export function registerJobTools(server) { register(server,"nas_job_list","List asynchronous NAS jobs.",{},()=>request("GET","/v1/jobs"),{readOnlyHint:true});register(server,"nas_job_get","Get an asynchronous NAS job.",{id:z.string().min(1)},({id})=>request("GET",`/v1/jobs/${encodeURIComponent(id)}`),{readOnlyHint:true});register(server,"nas_job_logs","Read job logs from an optional cursor.",{id:z.string().min(1),cursor:z.number().int().nonnegative().optional()},({id,cursor})=>request("GET",`/v1/jobs/${encodeURIComponent(id)}/logs${cursor===undefined?"":`?cursor=${cursor}`}`),{readOnlyHint:true});register(server,"nas_job_cancel","Cancel an asynchronous NAS job.",{id:z.string().min(1)},({id})=>request("POST",`/v1/jobs/${encodeURIComponent(id)}/cancel`,{}),{destructiveHint:true}) }
